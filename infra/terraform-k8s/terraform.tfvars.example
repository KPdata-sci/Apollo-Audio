# Copy to terraform.tfvars (gitignored) and fill in.

kubeconfig_path   = "~/.kube/config"
scraper_image     = "ghcr.io/<you>/apollo-scraper:latest" # see README — must be pullable by the cluster, not just built locally
postgres_password = "change-me"
